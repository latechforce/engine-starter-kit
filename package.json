{
  "name": "engine-stater-kit",
  "module": "src/start.ts",
  "type": "module",
  "scripts": {
    "format": "prettier --write .",
    "lint": "eslint . --fix",
    "test": "bun test --bail",
    "build": "bun build --entrypoints ./src/start.ts --outdir ./dist --target bun --packages external",
    "start": "bun --watch run src/start.ts"
  },
  "devDependencies": {
    "@eslint/js": "^9.20.0",
    "@types/bun": "latest",
    "eslint": "^9.20.1",
    "globals": "^15.15.0",
    "prettier": "^3.5.1",
    "typescript-eslint": "^8.24.1"
  },
  "peerDependencies": {
    "typescript": "^5.7.3"
  },
  "dependencies": {
    "@latechforce/engine": "latest"
  },
  "engines": {
    "bun": "1.2.2"
  }
}
